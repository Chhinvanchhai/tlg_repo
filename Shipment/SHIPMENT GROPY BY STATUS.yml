info:
  name: SHIPMENT GROPY BY STATUS
  type: http
  seq: 12

http:
  method: POST
  url: "{{baseurl}}/api_gateway/v1/models/shipment.tracking.md/get"
  body:
    type: json
    data: |-
      {
        "params" : {
              
        "groupby": "port_status,shipment_status",
        "keep_zero": false
      //     ,"domain": [["customer_id","=", 1]]
           
        }
        
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
