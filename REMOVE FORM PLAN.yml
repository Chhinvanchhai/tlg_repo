info:
  name: REMOVE FORM PLAN
  type: http
  seq: 11

http:
  method: POST
  url: "{{baseurl}}/api_gateway/v1/models/cs.control.log/remove_from_plan"
  body:
    type: json
    data: |-
      {
         "params" : {
          "active_ids": [186]
         }
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
