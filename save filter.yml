info:
  name: save filter
  type: http
  seq: 7

http:
  method: POST
  url: "{{baseurl}}/api_gateway/v1/models/cs.control.log/save_filter"
  body:
    type: json
    data: |-
      {
          "params": {
              "name": "Active Projects",
              "is_default": true
          }
      }

      //** note , do not save this api
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
