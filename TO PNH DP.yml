info:
  name: TO PNH DP
  type: http
  seq: 12

http:
  method: POST
  url: "{{baseurl}}/api_gateway/v1/models/cs.control.log/to_pnh_dp"
  body:
    type: json
    data: |-
      {
         "params" : {
          "active_ids": [186]
         }
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
