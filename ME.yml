info:
  name: ME
  type: http
  seq: 13

http:
  method: POST
  url: "{{baseurl}}/api_gateway/v1/me"
  body:
    type: json
    data: |-
      {
         "params" : {

         }
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
