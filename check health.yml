info:
  name: check health
  type: http
  seq: 5

http:
  method: POST
  url: "{{baseurl}}/api_gateway/v1/health"
  body:
    type: json
    data: |-
      {
         "params" : {

         }
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
