info:
  name: Get Profile
  type: http
  seq: 12

http:
  method: POST
  url: "{{baseurl}}/api_gateway/v1/models/res.users/profile"
  body:
    type: json
    data: |-
      {

      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
