info:
  name: get favorite filter
  type: http
  seq: 9

http:
  method: POST
  url: "{{baseurl}}/api_gateway/v1/models/cs.control.log/get_filters"
  body:
    type: json
    data: "{}"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
