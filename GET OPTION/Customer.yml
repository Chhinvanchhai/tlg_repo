info:
  name: Customer
  type: http
  seq: 13

http:
  method: POST
  url: "{{baseurl}}/api_gateway/v1/models/cs.control.log/selection"
  body:
    type: json
    data: |-
      {
          "params": {
              "field_name": "customer_id",
              "parent": {
                  "fields_name": "display_name,email"
             
              }
              ,"page": 1,
              "limit": 3
              // ,
              // "id": 922
         
          }
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
