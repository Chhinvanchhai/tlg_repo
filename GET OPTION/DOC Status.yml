info:
  name: DOC Status
  type: http
  seq: 16

http:
  method: POST
  url: "{{baseurl}}/api_gateway/v1/models/cs.control.log/selection"
  body:
    type: json
    data: |-
      {
          "params": {
              "field_name": "doc_status_id",
              "limit": 3,
              "page": 1
          }
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
